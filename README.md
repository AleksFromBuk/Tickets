# Tickets_perse
“очка входа - метод TicketParser.main(String[] args)
ѕараметры передаютс€ в формате -key=value, где key и value соответственно ключ 
(по ключу будет запрошен параметр) и значение (собственно сам параметр).
ѕредусмотрено три входных параметра с ключами: path, out и filter, где 
1. path: путь к файлу, содержащему источник данных(JSON);
2. out: приемник данных, в который будет выводитьс€ результат;
3. filter: фильтр по направлению авиабилетов, то есть пункт отправки('origin') 
и пункт прибыти€('destination'), таким образом определ€етс€ направление, 
по которому будет происходить анализ.

 ѕример входных данных: -path=data/source.json -out=result.txt -filter=LRN,TLV
 (следует обратить внимание, что значени€ дл€ ключа filter передаютс€ списком из двух
 элементов, разделенных зап€той)
 
 ¬ случае отсутстви€ входных параметров, источник данных(Tickets.json) расположен
в корне проекта(Tickets.json), аналогично и приЄмник (result.txt), а в качестве
целевого направлени€ выбираетс€ то, которое предполагаетс€ в исходной постановке,
то есть origin=VVO, destination=TLV
